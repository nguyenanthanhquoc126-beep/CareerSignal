Careersignal_dbt:
  outputs:

    dev:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos | gssapi | jwt | certificate | oauth | oauth_console}
      user: dbt
      database: nessie
      host: localhost
      port: 8085
      schema: gold
      threads: 2

    # prod:
    #   type: trino
    #   method: none  # optional, one of {none | ldap | kerberos | gssapi | jwt | certificate | oauth | oauth_console}
    #   user: [prod user]
    #   password: [prod password]  # required if method is ldap or kerberos
    #   database: [database name]
    #   host: [hostname]
    #   port: [port number]
    #   schema: [prod_schema]
    #   threads: [1 or more]

  target: dev